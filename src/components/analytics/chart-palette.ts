/** Brand ramp for funnel stages (top → bottom) and pie slices. */
export const FUNNEL_COLORS = [
  "hsl(var(--navy))",
  "hsl(var(--navy-700))",
  "hsl(var(--saffron))",
  "hsl(var(--sage))",
  "hsl(var(--terracotta))",
  "hsl(var(--muted-foreground))",
];
