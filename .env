# NUMU Merchant Hub - Environment Variables

# Backend API
VITE_API_URL=http://localhost:8021/api/v1

# Storefront URL template — use {subdomain} as placeholder
# Development: http://{subdomain}.localhost:8081
# Production:  https://{subdomain}.numu.io
VITE_STOREFRONT_URL=http://{subdomain}.localhost:8081
